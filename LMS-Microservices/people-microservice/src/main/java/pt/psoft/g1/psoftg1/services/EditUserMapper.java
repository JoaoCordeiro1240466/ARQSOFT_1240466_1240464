package pt.psoft.g1.psoftg1.services;

import org.mapstruct.Mapper;

/**
 * This mapper is intentionally left empty.
 * The mapping logic has been moved to the service layer (UserService.java)
 * to break circular dependencies and simplify the architecture.
 */
@Mapper(componentModel = "spring")
public interface EditUserMapper {
}
